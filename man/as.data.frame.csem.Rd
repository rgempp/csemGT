% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods-csem.R
\name{as.data.frame.csem}
\alias{as.data.frame.csem}
\title{Coerce a `csem` object to a data frame}
\usage{
\method{as.data.frame}{csem}(
  x,
  row.names = NULL,
  optional = FALSE,
  by = c("person", "score"),
  ...
)
}
\arguments{
\item{x}{A `csem` object.}

\item{row.names, optional}{Accepted for consistency with the
[base::as.data.frame()] generic; both are ignored.}

\item{by}{One of `"person"` (the default) or `"score"`. `"person"`
returns the per-person `$estimates` table; `"score"` returns the
`$by_score` table.}

\item{...}{Currently ignored.}
}
\value{
A data frame: `$estimates` when `by = "person"`, `$by_score`
  when `by = "score"`.
}
\description{
Returns one of the two wide-format tables carried by a `csem` object:
the person-level table (`by = "person"`, the default) or the
score-level table (`by = "score"`).
}
\examples{
set.seed(1)
d <- matrix(rbinom(60 * 12, 1, 0.5), nrow = 60)
fit <- csem_gt(d, error_type = "absolute")
head(as.data.frame(fit))                 # by = "person"
head(as.data.frame(fit, by = "score"))

}
\seealso{
[by_score()] for the score-level table through a dedicated
  accessor; [coef.csem()] for the variance components.
}
