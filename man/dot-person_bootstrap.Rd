% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-bootstrap.R
\name{.person_bootstrap}
\alias{.person_bootstrap}
\title{Person-resampling bootstrap for the GT CSEM estimators}
\usage{
.person_bootstrap(
  data,
  X,
  paradigm = "gt",
  method = c("full", "large_a", "uncorrelated"),
  error_type = c("absolute", "relative"),
  R = 1000L,
  seed = NULL,
  return_replicates = FALSE,
  verbose = FALSE
)
}
\arguments{
\item{data}{Numeric matrix \eqn{N \times J}.}

\item{X}{Numeric vector of length \eqn{N}; conditioning value per
person.}

\item{paradigm}{Character; currently must be `"gt"`.}

\item{method, error_type}{Resolved arguments of `csem_gt()`.}

\item{R}{Integer; number of bootstrap replications. Default 1000.}

\item{seed}{Integer or `NULL`; seed for reproducibility.}

\item{return_replicates}{Logical; if `TRUE`, also returns the
per-person mean of the replicate estimates in component
`$replicates`.}

\item{verbose}{Logical; emit progress messages every 100 persons.}
}
\value{
A list with the same shape as `.item_bootstrap()`.
}
\description{
Resamples persons with replacement, refits the variance components,
and recomputes the per-person CSEMs on every replicate. The
per-person sampling variance is then evaluated by aligning the
replicate estimates with the original person index (or, equivalently
for csem_gt, with the score level) and computing the empirical
variance across replicates. This is the canonical bootstrap of the
other paradigms (`csem_split_half`, `csem_anova`, `csem_binomial`)
and is provided here for forward compatibility with `csemR`.
}
\keyword{internal}
